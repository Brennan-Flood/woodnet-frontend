import React from "react";

const Header = (props) => {
    return (
        <header className="app-header">
            <h1 className="app-title">Woodnet Server Control Panel</h1>
        </header>
    )
};

export default Header;
